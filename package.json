{
  "name": "serverless-spike",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local-debug": "export SLS_DEBUG=* && node --inspect-brk ./node_modules/.bin/serverless invoke local --stage=dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless": "^1.35.1",
    "serverless-iam-roles-per-function": "^1.0.4"
  }
}
